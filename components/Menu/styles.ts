import styled from 'styled-components'

export const MenuButton = styled.button`
  position: fixed;
  width: 2rem;
  height: 2rem;
  background: none;
  border: none;
  top: 1rem;
  left: 1rem;
  z-index: 1;

  &:hover {
    cursor: pointer;
  }
`
